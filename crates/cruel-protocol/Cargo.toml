[package]
name = "cruel-protocol"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
jsonschema = { version = "0.33", default-features = false }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
typify = "0.7"

