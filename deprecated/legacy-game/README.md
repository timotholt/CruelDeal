# Deprecated Legacy Game

This directory contains the retired `/game` implementation.

It is preserved as a source archive and is intentionally excluded from the
active TypeScript build. The current gameplay system lives under `/play`,
`contexts/PlayGameContext.tsx`, and `services/playgame/`.

Do not import modules from this directory into active application code.
