# Deprecated TypeScript Location Catalog

`locations.ts` is the pre-Phase-1.5 executable catalog retained only as migration
history. No runtime, validator, generator, test, or tool may import it. Active
locations live under `location-sets/core-v1/locations/`.
