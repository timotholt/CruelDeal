# Deprecated Card Sets

These files are preserved for reference only. They are not loaded by `/play`,
`BOOTSTRAP_MANIFEST`, or the active card validator.

Active card authoring lives in:

```txt
services/playgame/engine/manifest/card-sets/core-v1/cards/<def-id>/card.json
```
